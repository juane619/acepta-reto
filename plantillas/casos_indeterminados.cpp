/*Numero de casos indeterminados, hasta leer EOF (CTRL+D, linux o CTRL+Z, windows)*/
#include <cstdio>

using namespace std;

int main() {
    int d;

    while (scanf("%d", &d) > 0) {

        printf("\n");
    }
    
    return 0;
}